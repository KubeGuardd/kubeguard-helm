apiVersion: v1
kind: Service
metadata:
  name: watcher-service-active
  namespace: {{ .Values.namespace }}
  labels:
    app: watcher-service
    role: active
    app.kubernetes.io/name: watcher-service
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app: watcher-service
    rollouts-pod-template-hash: active
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
---
apiVersion: v1
kind: Service
metadata:
  name: watcher-service-preview
  namespace: {{ .Values.namespace }}
  labels:
    app: watcher-service
    role: preview
    app.kubernetes.io/name: watcher-service
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app: watcher-service
    rollouts-pod-template-hash: preview
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}