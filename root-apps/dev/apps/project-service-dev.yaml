apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: project-service-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/KubeGuardd/kubeguard-helm.git
    targetRevision: main
    path: project-service
    helm:
      valueFiles:
        - values-dev.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: kubeguard-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=false